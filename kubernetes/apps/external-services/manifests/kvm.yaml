apiVersion: v1
kind: Service
metadata:
  name: kvm
  namespace: external-services
spec:
  type: ExternalName
  externalName: 10.9.0.10
  ports:
    - name: http
      port: 80
      targetPort: 80
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kvm-tls
  namespace: external-services
  annotations:
    avp.kubernetes.io/path: "secret/data/kubernetes/config"
spec:
  secretName: kvm-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - kvm.<domain>
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: kvm
  namespace: external-services
  annotations:
    avp.kubernetes.io/path: "secret/data/kubernetes/config"
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`kvm.<domain>`)
      kind: Rule
      services:
        - name: kvm
          port: 80
  tls:
    secretName: kvm-tls
