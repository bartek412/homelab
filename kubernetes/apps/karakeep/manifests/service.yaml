apiVersion: v1
kind: Service
metadata:
  name: karakeep
  namespace: karakeep
spec:
  type: ClusterIP
  selector:
    app: karakeep
  ports:
    - name: web
      port: 80
      targetPort: 3000
      protocol: TCP
